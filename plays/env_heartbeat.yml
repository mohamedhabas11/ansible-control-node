---
- hosts: all
  gather_facts: false
  tasks:
    - name: "Confirm SSH access on all hosts"
      ansible.builtin.command: echo "SSH access confirmed on {{ inventory_hostname }}"
      changed_when: false